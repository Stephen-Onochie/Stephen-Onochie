node_modules
.env
.env*.local
.next/
*.tsbuildinfo

/cache/
/docs/
.vercel

# Agent skills (installed locally via `npx skills add`)
.agents/

# Local agent / tooling state (Claude Code, lavish review artifacts)
.claude/
.lavish/
skills-lock.json
